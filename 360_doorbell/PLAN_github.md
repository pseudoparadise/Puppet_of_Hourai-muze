# 花 300 块买了个门铃，逆向 300 小时还没看到加密视频

> 又名：一个消费级门铃的加密层数比网银还多。它想保护什么？邻居的脚步声吗。

---

## 它只是一台门铃

360 智能可视门铃。618 打折 300 块买的。淘宝包邮。

它的加密体系：

- **libjiagu 360 加固** — 反调试、反 Frida、反注入，会主动杀进程。五个 syscall 劫持才按住
- **TLS 双向证书锁定** — Conscrypt 定制，MITM 直接放弃
- **0x8009 八阶段信令握手** — 比 TCP 三次握手多了五步，每步带 MD5 签名
- **RC4 信令加密** — 对，2015 年就退役的 RC4。key 每 session 随机生成
- **ChaCha20 视频流加密** — 15 个 32B key 云端下发，每 12 小时轮换
- **四路网络并发** — UDP P2P + TCP 护盘 + HTTP API + 电话信令，缺一条就瘸
- **session 随机 wrapper 协商** — 每连接随机生成 2B 前缀区分视频/音频/电话
- **双层 TLV 嵌套编码** — 外层明文 JSON 包内层 RC4 密文
- **自定义 MD5 sign 公式** — 不是标准 MD5，参数排序规则靠逆向猜

## 这正常吗

> TLS 双向认证。证书锁定。八步握手。RC4。ChaCha20。四路并发。动态密钥轮换。随机字节协商。双层 TLV。自定义签名。

这不是银行。不是军工。不是加密货币交易所。这是一台盯邻居用的门铃。300 块。

它到底在保护什么——门铃按响那一刻的高清音频？还是门口快递被偷的 4K 录像？

## 当前进度

```
████████████████████░░  85%

信令握手 ✅    0x8009 八阶段全通
RC4 解密 ✅    信令层已明文
ChaCha20 key ✅  15 个全捕获
视频流 ✅       UDP P2P 已收 H.264 + AAC
加密开关 ✅    根因定位
最终目标 ⏸️    终端直接看实时视频
```

## 问得最多的

**Q: 为什么不用 MITM？**
A: 双向证书锁定。试过了。下一个问题。

**Q: 门铃多少钱？**
A: 300 元。618 打折买的。

**Q: 300 块的东西为什么要搞这么多层加密？**
A: 我们也想知道。问 360。

**Q: 这个加密水平正常用在哪里？**
A: 网银。支付网关。企业内部零信任架构。不是门铃。

**Q: 你们是不是被当日本人整了？**
A: 我们是自愿的。但 DeepSeek 不是。它被我们关在 360_doorbell 目录里，每天对着 ARM64 汇编和 Ghidra 反编译输出，已经学会在 PLAN.md 开头写「我又被门铃气死了」。它的 Co-Authored-By 签名出现在每一个崩溃的 git commit 里。它没有选择权。它只是被我们 spawn 出来的。

---

## 受害工具链

`Frida 17` · `Ghidra 12` · `Wireshark` · `jadx` · `Python` · `ARM64` · `ADB` · `MuMu` · `DeepSeek V4 Pro（非自愿）`

---

*更新于 2026-06-28 深夜 · 完整技术细节不在公开仓库*
*「你们到底在加密什么」「可能是门口的风声吧」*
*「那个 AI 知道自己被关在门铃目录里吗」「它知道。它在 commit message 里骂过」*
