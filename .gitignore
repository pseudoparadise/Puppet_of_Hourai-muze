# ── Python cache ──
__pycache__/
*.pyc
*.pyo
*$py.class

# ── 敏感文件（API Key、私人对话、状态数据） ──
config.json
config.yaml
chat_logs.json
chat_logs_backup.json
chat_logs_work.json
state.json
trigger.log
trigger_error.log

# ── 向量数据库与索引 ──
cards.db
vectors.faiss
id_map.json
model_meta.json

# ── 待审核卡片与锚定集（私人记忆） ──
pending_cards.json
anchor_set.json

# ── 日记与滚动总结（私人内容） ──
diary/
rolling_summary.md

# ── AI 提示词（写卡/裁决/日记 prompt） ──
delegate/prompts/
persona/prompt_v1.txt
persona/prompt_v1_base.txt
persona/backups/
persona/miner_state.json

# ── 音乐状态缓存 ──
.music_state.json
music_history.jsonl
music_debug.log
memory/music_history.jsonl
memory/music_debug.log

# ── VA 日志 ──
memory/va_log.jsonl

# ── 手动 VA 先验（私人生理/情绪数据） ──
manual_va.json

# ── Persona 情感状态（私人情感数据） ──
memory/persona_state.json

# ── DS 自省日记（私密反思内容） ──
memory/reflections/

# ── 数据库 / 索引备份（含私人 embedding） ──
memory/cards_backup_*.db
memory/id_map_backup_*.json
memory/vectors_backup_*.faiss
memory/model_meta_backup_*.json

# ── 光遇钢琴定点（非敏感） ──
# sky_keys.json

# ── 进程 PID 文件 ──
.daemon.pid
.polling_loop.pid
.music_sync.pid

# ── 会话状态（含私人对话上下文） ──
.session_state.json

# ── 待办提醒状态 ──
reminded_todos.json
memory/reminded_todos.json

# ── 划卡审计日志 ──
memory/resolution_log.jsonl

# ── 崩溃日志 ──
console_crash.log

# ── 聊天日志轮转 ──
chat_logs_*.json

# ── 损坏文件备份 ──
*.corrupted_*

# ── Claude Code ──
.claude/

# ── 快照与诊断文件 ──
状态快照_*.txt
查看状态.bat

# ── 系统垃圾 ──
%SystemDrive%/

# ── 家/工位 PS1 启动脚本（含 API Key） ──
家.ps1
工位.ps1

# ── MuMu 广告歼灭战工具（2026-06-03） ──
mumu_hook.c
mumu_hook.dll
mumu_adfree.py
mumu_memwipe.py
mumu_nuke.py
mumu_html_ads.py
fake_ad_server.py
catch_the_ghost.py
mumu_ui.xml
_vm0.txt
_vm1.txt
block_mumu_ads.ps1
lawnchair_orig.apk
_scan_urls.py
_scan_bin.py
_patch_rcc.py
_patch_vip.py
_mumu_launcher.py
_mumu_hide_vip.py
_test_file.py
_rcc_patcher.py

# ── 调试日志 ──
ghost_hunt.log

# ── 模式文件（本地不提交） ──
.mode

# ── IO Feedling（含 API Key，不提交） ──
io/
.feedling.env

# ── Proactive gate 数据（含位置/健康信息，不提交） ──
shortcut_data/
proactive_state.json

# ── IDE ──
.vscode/
.idea/
*.swp
*.swo
*.swn
